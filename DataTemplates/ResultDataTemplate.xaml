<?xml version="1.0" encoding="UTF-8"?>
<DataTemplate xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
              xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:local="clr-namespace:MauiAccessibilityRepro"
              x:Class="MauiAccessibilityRepro.DataTemplates.ResultDataTemplate"
              x:DataType="local:ResultItem">
    <VerticalStackLayout>
        <VerticalStackLayout Padding="15,10">
            <Grid ColumnDefinitions="*,100">
                <Label Grid.Column="0"
                       Text="{Binding TournamentName}"
                       FontSize="16"
                       HorizontalTextAlignment="Start" />
                <Label Grid.Column="1"
                       LineBreakMode="NoWrap"
                       HorizontalTextAlignment="End"
                       MinimumHeightRequest="40"
                       Text="{Binding Position}"
                       FontSize="24" />
            </Grid>
            <Grid ColumnDefinitions="*,*,*">
                <Label Grid.Column="0"
                       LineBreakMode="NoWrap"
                       HorizontalTextAlignment="Start"
                       FontSize="12"
                       Text="{Binding EventName}"
                       TextColor="Gray" />
                <Label Grid.Column="1"
                       Text="{Binding EventDate, StringFormat='{0:d}'}"
                       FontSize="12"
                       TextColor="Gray"
                       HorizontalTextAlignment="Center" />
                <Label Grid.Column="2"
                       LineBreakMode="NoWrap"
                       HorizontalTextAlignment="End"
                       FontSize="12"
                       Text="{Binding CurrentPoints, StringFormat='{0:0.00}'}"
                       TextColor="Gray" />
            </Grid>
        </VerticalStackLayout>
        <BoxView HeightRequest="1" Color="LightGray" />
    </VerticalStackLayout>
</DataTemplate>
